{
  "name": "workshop-edge",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "bun run scripts/lint.ts",
    "test": "bun test",
    "build": "bun build ./src/functions/auth-cpf.ts ./src/functions/notify.ts --outdir dist --target bun",
    "package:lambdas": "bash ./scripts/package-lambdas.sh"
  },
  "engines": {
    "bun": ">=1.3.6"
  }
}

