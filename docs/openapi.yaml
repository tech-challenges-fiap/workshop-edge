openapi: 3.0.3
info:
  title: Workshop Edge — External API
  version: 1.0.0
  description: |
    External-facing API for the workshop system, served via AWS HTTP API Gateway (workshop-edge).

    This document covers the complete public API surface:
    - `POST /auth/login` — CPF-based authentication; issues HS256 JWT tokens
    - `POST /notify` — outbound notification delivery (email / SMS)
    - `/api/**` — all application routes (stock items, services, service tasks, work orders, vehicles, people, webhooks)

    ## Authentication flow

    1. Client calls `POST /auth/login` with a CPF and receives a Bearer token (valid 900 s).
    2. Client includes `Authorization: Bearer <token>` on every protected `/api/*` request.
    3. The token is validated by `workshop-app` (same shared HS256 secret).

    ## CORS

    Allowed headers: `Authorization`, `Content-Type`, `X-Request-Id`
    Allowed methods: `GET POST PUT PATCH DELETE OPTIONS`
    Allowed origins: configurable (default `*`)

servers:
  - url: https://nach89wgoa.execute-api.sa-east-1.amazonaws.com/stag
    description: Staging (AWS HTTP API Gateway)
  - url: https://93telx7g0g.execute-api.sa-east-1.amazonaws.com/prod
    description: Production (AWS HTTP API Gateway)

paths:
  /auth/login:
    post:
      tags:
        - Auth
      summary: Authenticate via CPF
      description: |
        Looks up a person by CPF in the workshop database, enforces `status = active`,
        and returns a signed HS256 JWT.

        CPF is accepted with or without formatting (dots/dashes are stripped before lookup).

        **JWT claims returned:** `sub`, `person_id`, `cpf` (digits only), `role`, `status`,
        `iss` (`workshop-edge`), `aud` (`workshop-app`), `iat`, `exp`, `jti`.

        **Errors:**
        - 400 — CPF missing or has wrong shape (not 11 digits after stripping)
        - 401 — person not found
        - 403 — person found but `status ≠ active`
        - 405 — non-POST request
        - 500 — database or JWT secret failure
      operationId: loginByCpf
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthLoginRequest"
            examples:
              formatted:
                summary: CPF with formatting
                value:
                  cpf: "123.456.789-00"
              raw:
                summary: CPF digits only
                value:
                  cpf: "12345678900"
      responses:
        "200":
          description: JWT token issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthLoginResponse"
              example:
                token_type: Bearer
                access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                expires_in: 900
        "400":
          description: Invalid CPF format
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
              example:
                error: ValidationError
                details:
                  - path: [cpf]
                    message: CPF must have 11 digits
                    code: invalid_string
        "401":
          description: Person not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error: Unauthorized
                message: Person not found
        "403":
          description: Person status is not active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error: Forbidden
                message: Person is not active
        "405":
          description: Method not allowed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Unexpected error (database or JWT secret failure)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /notify:
    post:
      tags:
        - Notifications
      summary: Send notification
      description: |
        Accepts a notification request and forwards it to the configured webhook provider
        (`NOTIFICATION_WEBHOOK_URL`). Returns 202 immediately.

        **Two accepted formats:**

        **Normalized** — explicit channel + destination:
        ```json
        { "channel": "email", "destination": "user@example.com", "message": "..." }
        ```

        **App-style** — email and/or phone in top-level fields:
        ```json
        { "email": "user@example.com", "phone": "+5511988887777", "message": "..." }
        ```
        App-style generates one delivery per present channel (email → email, phone → sms).

        If `NOTIFICATION_WEBHOOK_URL` is not set the request is accepted silently (no external call).
        If the webhook returns a non-2xx status, the endpoint returns 502.

        **Errors:**
        - 400 — missing `message`, invalid `channel`, or no destination provided
        - 405 — non-POST request
        - 500 — internal parsing failure
        - 502 — webhook provider returned error
      operationId: sendNotification
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NotifyRequest"
            examples:
              normalized:
                summary: Normalized format (single channel)
                value:
                  channel: email
                  destination: user@example.com
                  message: "Your vehicle is ready for pickup."
              appStyleBoth:
                summary: App-style (email + SMS)
                value:
                  email: user@example.com
                  phone: "+5511988887777"
                  message: "Your work order is awaiting approval."
              appStyleEmail:
                summary: App-style (email only)
                value:
                  email: user@example.com
                  message: "Diagnosis complete."
      responses:
        "202":
          description: Notification accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotifyResponse"
              example:
                message: notification accepted
                accepted: true
                deliveries:
                  - channel: email
                    destination: user@example.com
                  - channel: sms
                    destination: "+5511988887777"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "405":
          description: Method not allowed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Notification provider returned an error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error: NotificationDeliveryFailed
                message: Webhook provider returned non-2xx status

  /api/health:
    get:
      tags:
        - Health
      summary: Health check
      operationId: getHealth
      security: []
      responses:
        "200":
          description: Healthy server
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

  /api/stock-items:
    get:
      tags:
        - Stock Items
      summary: List stock items
      operationId: listStockItems
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of stock items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/StockItem"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

    post:
      tags:
        - Stock Items
      summary: Create stock item
      operationId: createStockItem
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateStockItemRequest"
      responses:
        "201":
          description: Stock item created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockItem"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "409":
          description: SKU already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/stock-items/{id}:
    get:
      tags:
        - Stock Items
      summary: Get stock item by ID
      operationId: getStockItemById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the stock item
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Stock item found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockItem"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Stock item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

    put:
      tags:
        - Stock Items
      summary: Update stock item catalog data
      operationId: updateStockItem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the stock item
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateStockItemRequest"
      responses:
        "200":
          description: Stock item updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockItem"
        "400":
          description: Body validation error or invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Stock item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

    delete:
      tags:
        - Stock Items
      summary: Delete stock item
      operationId: deleteStockItem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the stock item
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Stock item successfully deleted
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Stock item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/stock-items/{id}/increase:
    post:
      tags:
        - Stock Items
      summary: Increase stock item quantity
      operationId: increaseStockItemQuantity
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StockItemQuantityChangeRequest"
      responses:
        "200":
          description: Quantity updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockItem"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Stock item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/stock-items/{id}/consume:
    post:
      tags:
        - Stock Items
      summary: Consume stock item quantity
      operationId: consumeStockItemQuantity
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StockItemQuantityChangeRequest"
      responses:
        "200":
          description: Quantity updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockItem"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Stock item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Insufficient stock
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/services:
    post:
      tags:
        - Services
      summary: Create service
      operationId: createService
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrUpdateServiceRequest"
      responses:
        "201":
          description: Service created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
      security:
        - bearerAuth: []

    get:
      tags:
        - Services
      summary: List services
      operationId: listServices
      responses:
        "200":
          description: List of services
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Service"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
      security:
        - bearerAuth: []

  /api/services/{id}:
    get:
      tags:
        - Services
      summary: Get service by ID
      operationId: getServiceById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service
      responses:
        "200":
          description: Service found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []
    put:
      tags:
        - Services
      summary: Update service
      operationId: updateService
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrUpdateServiceRequest"
      responses:
        "200":
          description: Service updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "400":
          description: Body validation error or invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

    delete:
      tags:
        - Services
      summary: Delete service
      operationId: deleteService
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service
      responses:
        "204":
          description: Service successfully deleted
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/services/{id}/average-duration:
    get:
      tags:
        - Services
      summary: Calculate average service duration based on completed ServiceTasks
      operationId: getServiceAverageDuration
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service
      responses:
        "200":
          description: Average duration calculated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceAverageDurationResponse"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/service-tasks:
    get:
      tags:
        - Service Tasks
      summary: List service tasks
      operationId: listServiceTasks
      responses:
        "200":
          description: List of service tasks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceTask"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

    post:
      tags:
        - Service Tasks
      summary: Create service task
      operationId: createServiceTask
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceTaskRequest"
      responses:
        "201":
          description: Service task created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Business rule violated when creating service task (WorkOrder is not in DIAGNOSIS)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/service-tasks/{id}:
    get:
      tags:
        - Service Tasks
      summary: Get service task by ID
      operationId: getServiceTaskById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      responses:
        "200":
          description: Service task found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/service-tasks/{id}/approve:
    post:
      tags:
        - Service Tasks
      summary: Approve service task
      operationId: approveServiceTask
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      responses:
        "200":
          description: Service task approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition or finalization rule violated (ServiceTasks not completed/canceled)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/service-tasks/{id}/reject:
    post:
      tags:
        - Service Tasks
      summary: Reject service task
      operationId: rejectServiceTask
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      responses:
        "200":
          description: Service task rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/service-tasks/{id}/start-execution:
    post:
      tags:
        - Service Tasks
      summary: Start service task execution
      operationId: startServiceExecution
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartServiceExecutionRequest"
        description: Optional body; if omitted, the current server time is used.
      responses:
        "200":
          description: Service task in execution
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service task or stock item not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Business conflict (insufficient stock or invalid transition)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/service-tasks/{id}/complete:
    post:
      tags:
        - Service Tasks
      summary: Complete service task execution
      operationId: completeServiceTask
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteServiceTaskRequest"
      responses:
        "200":
          description: Service task completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/work-orders:
    get:
      tags:
        - Work Orders
      summary: List work orders
      description: |
        Returns the operational queue for workshop execution.
        Includes only `IN_EXECUTION`, `WAITING_APPROVAL`, `DIAGNOSIS`, and `RECEIVED`.
        Results are ordered by status priority, then oldest `updatedAt`, then ascending `id`.
      operationId: listWorkOrders
      responses:
        "200":
          description: List of work orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkOrder"
              examples:
                operationalQueue:
                  summary: Queue ordered by operational priority
                  value:
                    - id: 23
                      vehicleId: 11
                      status: IN_EXECUTION
                      totalAmount: 520
                      publicToken: 5adf8240-af31-4f72-8b88-a7fd5f5f45bb
                      publicTokenExpiresAt: "2099-01-10T12:00:00.000Z"
                      createdAt: "2024-01-10T09:20:00.000Z"
                      updatedAt: "2024-01-10T11:00:00.000Z"
                      serviceTasks: []
                    - id: 19
                      vehicleId: 7
                      status: WAITING_APPROVAL
                      totalAmount: 340
                      publicToken: c99d11f7-d806-4a29-a179-65fe718d503d
                      publicTokenExpiresAt: "2099-01-10T12:00:00.000Z"
                      createdAt: "2024-01-10T09:00:00.000Z"
                      updatedAt: "2024-01-10T10:00:00.000Z"
                      serviceTasks: []
                    - id: 27
                      vehicleId: 12
                      status: DIAGNOSIS
                      totalAmount: 0
                      publicToken: 22b73fdc-36bf-4ab5-b1c4-4ec15b42bb97
                      publicTokenExpiresAt: "2099-01-10T12:00:00.000Z"
                      createdAt: "2024-01-10T09:10:00.000Z"
                      updatedAt: "2024-01-10T07:00:00.000Z"
                      serviceTasks: []
                    - id: 31
                      vehicleId: 15
                      status: RECEIVED
                      totalAmount: 0
                      publicToken: 8f49442f-d1d1-480e-bef6-5ab0b95acdb6
                      publicTokenExpiresAt: "2099-01-10T12:00:00.000Z"
                      createdAt: "2024-01-10T09:40:00.000Z"
                      updatedAt: "2024-01-10T06:00:00.000Z"
                      serviceTasks: []
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

    post:
      tags:
        - Work Orders
      summary: Create work order
      description: |
        Opens a work order using the full payload contract (`customer`, `vehicle`, `parts`, `services`).
        This endpoint returns `409` for uniqueness conflicts on customer document, vehicle plate, or part SKU.
      operationId: createWorkOrder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkOrderRequest"
            examples:
              fullPayload:
                summary: Full opening payload
                value:
                  customer:
                    name: Maria Souza
                    document: "16899535009"
                    phone: "+5511988887777"
                    email: maria.souza@example.com
                    role: customer
                  vehicle:
                    plate: FUL-9123
                    brand: Toyota
                    model: Corolla
                    year: 2021
                  parts:
                    - sku: SKU-FULL-DB-1
                      name: Air Filter
                      quantity: 8
                      price: 40
                  services:
                    - name: Revision
                      estimatedTime: 120
                      price: 300
                      requiredParts:
                        - sku: SKU-FULL-DB-1
                          quantity: 1
      responses:
        "201":
          description: Work order created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Body validation error or invalid payload references
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ValidationErrorResponse"
                  - $ref: "#/components/schemas/DomainErrorResponse"
              examples:
                validationError:
                  summary: Structural body validation failure
                  value:
                    error: ValidationError
                    details:
                      - path:
                          - services
                        message: "Too small: expected array to have >=1 items"
                        code: too_small
                payloadValidationError:
                  summary: Cross-field payload rule violation
                  value:
                    error: CreateWorkOrderPayloadValidationError
                    message: Service "Brake Service" references unknown SKU "SKU-UNKNOWN"
        "409":
          description: Uniqueness conflict on customer document, vehicle plate, or part SKU
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
              examples:
                duplicatePersonDocument:
                  summary: Existing customer document
                  value:
                    error: PersonDocumentAlreadyExists
                    message: Person with this document already exists
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/work-orders/{id}:
    get:
      tags:
        - Work Orders
      summary: Get work order by ID
      operationId: getWorkOrderById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the work order
      responses:
        "200":
          description: Work order found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Work order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/work-orders/{id}/cancel:
    post:
      tags:
        - Work Orders
      summary: Cancel work order
      operationId: cancelWorkOrder
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the work order
      responses:
        "200":
          description: Work order canceled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Work order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/work-orders/{id}/deliver:
    post:
      tags:
        - Work Orders
      summary: Deliver vehicle and close work order
      operationId: deliverVehicle
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the work order
      responses:
        "200":
          description: Work order delivered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Work order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/work-orders/{id}/diagnosis/start:
    post:
      tags:
        - Work Orders
      summary: Start work order diagnosis
      operationId: startWorkOrderDiagnosis
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the work order
      responses:
        "200":
          description: Diagnosis started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Work order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/work-orders/{id}/diagnosis/complete:
    post:
      tags:
        - Work Orders
      summary: Complete work order diagnosis
      operationId: completeWorkOrderDiagnosis
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the work order
      responses:
        "200":
          description: Diagnosis completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Work order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

  /api/public/work-orders/{token}:
    get:
      tags:
        - Work Orders
      summary: Get work order by public token
      operationId: getWorkOrderByPublicToken
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            description: Public token of the work order
      security: []
      responses:
        "200":
          description: Work order found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrder"
        "400":
          description: Token validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Public token not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Public token expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/public/work-orders/{token}/status:
    get:
      tags:
        - Work Orders
      summary: Get work order status by public token
      operationId: getWorkOrderStatusByPublicToken
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            description: Public token of the work order
      security: []
      responses:
        "200":
          description: Work order status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkOrderStatusResponse"
        "400":
          description: Token validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Public token not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Public token expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/public/work-orders/{token}/approval:
    get:
      tags:
        - Work Orders
      summary: Public work order approval page
      operationId: getWorkOrderApprovalPage
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            description: Public token of the work order
      security: []
      responses:
        "200":
          description: Rendered approval page
          content:
            text/html:
              schema:
                type: string
              examples:
                approvalPage:
                  summary: Approval page example
                  value: '<!doctype html><html lang="en"><head><meta charset="utf-8" /></head><body><h1>Work Order 1</h1><ul><li>Task 1</li></ul></body></html>'
        "400":
          description: Token validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Public token not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Public token expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/public/work-orders/{token}/service-tasks/{id}/approve:
    post:
      tags:
        - Service Tasks
      summary: Approve service task via public token
      operationId: approveServiceTaskByPublicToken
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            description: Public token of the work order
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      security: []
      responses:
        "200":
          description: Service task approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Token or ID validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Public token or service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Public token expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/public/work-orders/{token}/service-tasks/{id}/reject:
    post:
      tags:
        - Service Tasks
      summary: Reject service task via public token
      operationId: rejectServiceTaskByPublicToken
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
            description: Public token of the work order
        - name: id
          in: path
          required: true
          schema:
            type: string
            description: Numeric identifier of the service task
      security: []
      responses:
        "200":
          description: Service task rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTask"
        "400":
          description: Token or ID validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Public token or service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Public token expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"

  /api/webhooks/work-orders/events:
    post:
      tags:
        - Webhooks
      summary: Receive external work-order events
      description: |
        Unified inbound webhook for approval, rejection, and status-update events.
        This endpoint is intentionally unauthenticated in MVP and applies strict payload validation.
        Duplicate `eventId` values are idempotent and return a duplicate result without replaying side effects.
      operationId: handleExternalWorkOrderEvent
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExternalWebhookWorkOrderEventRequest"
            examples:
              serviceTaskApproved:
                summary: Approve service task
                value:
                  eventId: evt-approve-1
                  eventType: SERVICE_TASK_APPROVED
                  workOrderId: 10
                  serviceTaskId: 21
                  occurredAt: "2024-01-10T10:00:00.000Z"
              serviceTaskRejected:
                summary: Reject service task
                value:
                  eventId: evt-reject-1
                  eventType: SERVICE_TASK_REJECTED
                  workOrderId: 10
                  serviceTaskId: 22
                  occurredAt: "2024-01-10T10:05:00.000Z"
              statusUpdated:
                summary: Advance work order to diagnosis
                value:
                  eventId: evt-status-1
                  eventType: WORK_ORDER_STATUS_UPDATED
                  workOrderId: 10
                  targetStatus: DIAGNOSIS
                  occurredAt: "2024-01-10T11:00:00.000Z"
      responses:
        "200":
          description: Event processed or recognized as duplicate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExternalWebhookWorkOrderEventResponse"
              examples:
                processed:
                  summary: New event applied
                  value:
                    eventId: evt-approve-1
                    eventType: SERVICE_TASK_APPROVED
                    result: processed
                duplicate:
                  summary: Duplicate event id ignored
                  value:
                    eventId: evt-approve-1
                    eventType: SERVICE_TASK_APPROVED
                    result: duplicate
        "400":
          description: Invalid payload or invalid event data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
              examples:
                missingRequiredField:
                  summary: Invalid request body
                  value:
                    error: ValidationError
                    message: Invalid input
                    details:
                      - path:
                          - serviceTaskId
                        message: Invalid input
                        code: invalid_type
                missingServiceTaskIdForTransition:
                  summary: Invalid event for target transition
                  value:
                    error: ValidationError
                    message: "serviceTaskId is required for WORK_ORDER_STATUS_UPDATED to IN_EXECUTION"
                    details: []
        "404":
          description: Referenced work order or service task not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
              examples:
                workOrderNotFound:
                  summary: Target resource does not exist
                  value:
                    error: WorkOrderNotFound
                    message: Work order with id 999 was not found
        "409":
          description: Invalid status transition or stock conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
              examples:
                transitionNotAllowed:
                  summary: Requested transition conflicts with current status
                  value:
                    error: WorkOrderStatusTransitionNotAllowed
                    message: Transition from RECEIVED to DELIVERED is not allowed
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
              examples:
                unexpected:
                  summary: Unexpected processing failure
                  value:
                    error: UnexpectedError
                    message: Unexpected error

  /api/vehicles:
    post:
      tags:
        - Vehicles
      summary: Create vehicle
      operationId: createVehicle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVehicleRequest"
            example:
              plate: ABC1D23
              brand: Fiat
              model: Fiat Uno
              year: 2015
              ownerPersonId: 1
      responses:
        "201":
          description: Vehicle created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleValidationErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "409":
          description: License plate already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "500":
          description: Internal error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
      security:
        - bearerAuth: []

    get:
      tags:
        - Vehicles
      summary: List vehicles
      operationId: listVehicles
      responses:
        "200":
          description: List of vehicles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Vehicle"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "500":
          description: Internal error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
      security:
        - bearerAuth: []

  /api/vehicles/by-plate/{plate}:
    get:
      tags:
        - Vehicles
      summary: Get vehicle by plate
      operationId: getVehicleByPlate
      parameters:
        - name: plate
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Vehicle found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Invalid license plate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleValidationErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "500":
          description: Internal error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
      security:
        - bearerAuth: []

  /api/vehicles/{id}:
    get:
      tags:
        - Vehicles
      summary: Get vehicle by ID
      operationId: getVehicleById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vehicle found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: Invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "404":
          description: Vehicle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
        "500":
          description: Internal error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleErrorResponse"
      security:
        - bearerAuth: []

  /api/person:
    post:
      tags:
        - People
      summary: Create person
      operationId: createPerson
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePersonRequest"
      responses:
        "201":
          description: Person created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          description: Body validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "409":
          description: Document already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

    get:
      tags:
        - People
      summary: List people
      operationId: listPersons
      responses:
        "200":
          description: List of people
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Person"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []
  /api/person/{id}:
    get:
      tags:
        - People
      summary: Get person by ID
      operationId: getPersonById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Person found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Person not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

    put:
      tags:
        - People
      summary: Update person
      operationId: updatePerson
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePersonRequest"
      responses:
        "200":
          description: Person updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          description: Body validation error or invalid ID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Person not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "409":
          description: Document already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

    delete:
      tags:
        - People
      summary: Delete person
      operationId: deletePerson
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Person successfully deleted
        "400":
          description: Validation error (Invalid ID)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "404":
          description: Person not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "401":
          description: Unauthorized (missing or invalid JWT)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainErrorResponse"
        "500":
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnexpectedErrorResponse"
      security:
        - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: HS256 JWT issued by POST /auth/login (valid 900 s). Claims include sub, person_id, cpf, role, status.

  schemas:
    AuthLoginRequest:
      type: object
      properties:
        cpf:
          type: string
          description: CPF with or without formatting (dots/dashes are stripped before lookup)
          example: "123.456.789-00"
      required:
        - cpf

    AuthLoginResponse:
      type: object
      properties:
        token_type:
          type: string
          example: Bearer
        access_token:
          type: string
          description: Signed HS256 JWT
        expires_in:
          type: integer
          description: Token lifetime in seconds
          example: 900
      required:
        - token_type
        - access_token
        - expires_in

    NotifyRequest:
      description: Normalized or app-style notification payload
      oneOf:
        - $ref: "#/components/schemas/NotifyNormalizedRequest"
        - $ref: "#/components/schemas/NotifyAppStyleRequest"

    NotifyNormalizedRequest:
      type: object
      description: Explicit channel + destination
      properties:
        channel:
          type: string
          enum: [email, sms]
        destination:
          type: string
          description: Email address (channel=email) or phone number (channel=sms)
        message:
          type: string
          minLength: 1
      required:
        - channel
        - destination
        - message
      additionalProperties: false

    NotifyAppStyleRequest:
      type: object
      description: App-style; generates one delivery per present channel field
      properties:
        email:
          type: string
          format: email
        phone:
          type: string
        message:
          type: string
          minLength: 1
      required:
        - message

    NotifyDelivery:
      type: object
      properties:
        channel:
          type: string
          enum: [email, sms]
        destination:
          type: string
      required:
        - channel
        - destination

    NotifyResponse:
      type: object
      properties:
        message:
          type: string
          example: notification accepted
        accepted:
          type: boolean
          example: true
        deliveries:
          type: array
          items:
            $ref: "#/components/schemas/NotifyDelivery"
      required:
        - message
        - accepted
        - deliveries

    ValidationErrorResponse:
      type: object
      properties:
        error:
          type: string
          example: ValidationError
        details:
          type: array
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  oneOf:
                    - type: string
                    - type: integer
              message:
                type: string
              code:
                type: string
            additionalProperties: true
      required:
        - error
        - details

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required:
        - error
        - message

    # Stock items
    StockItem:
      type: object
      properties:
        id:
          type: string
          nullable: true
          description: Internal stock item ID
        sku:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        unitOfMeasure:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int32
          minimum: 0
        price:
          type: number
          minimum: 0
          description: Unit price of the stock item (up to two decimal places)
      required:
        - sku
        - name
        - quantity
        - price

    CreateStockItemRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int32
          minimum: 0
        unitOfMeasure:
          type: string
          nullable: true
        price:
          type: number
          minimum: 0
          description: Unit price of the stock item (up to two decimal places)
      required:
        - sku
        - name
        - quantity
        - price

    UpdateStockItemRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true
        unitOfMeasure:
          type: string
          nullable: true
        price:
          type: number
          minimum: 0
          description: Unit price of the stock item (up to two decimal places)
      additionalProperties: false

    StockItemQuantityChangeRequest:
      type: object
      properties:
        quantity:
          type: integer
          format: int32
          minimum: 1
      required:
        - quantity

    # Services
    ServiceRequiredItem:
      type: object
      properties:
        stockItemId:
          type: string
        quantity:
          type: integer
          format: int32
          minimum: 1
      required:
        - stockItemId
        - quantity

    Service:
      type: object
      properties:
        id:
          type: string
          nullable: true
        name:
          type: string
        estimatedTime:
          type: integer
          format: int32
          minimum: 1
          description: Estimated time in minutes
        price:
          type: number
          minimum: 0
          description: Base service price (up to two decimal places)
        requiredItems:
          type: array
          items:
            $ref: "#/components/schemas/ServiceRequiredItem"
      required:
        - name
        - estimatedTime
        - price
        - requiredItems

    ServiceAverageDurationResponse:
      type: object
      properties:
        serviceId:
          type: integer
          format: int32
          minimum: 1
          description: Service identifier
        averageDurationSeconds:
          type: integer
          format: int64
          nullable: true
          description: Average duration in seconds of completed ServiceTasks
        taskCount:
          type: integer
          format: int32
          minimum: 0
          description: Number of ServiceTasks considered in the calculation
        hasData:
          type: boolean
          description: Indicates whether there were completed ServiceTasks for calculation
      required:
        - serviceId
        - averageDurationSeconds
        - taskCount
        - hasData

    CreateOrUpdateServiceRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        estimatedTime:
          type: integer
          format: int32
          minimum: 1
        price:
          type: number
          minimum: 0
          description: Base service price (up to two decimal places)
        requiredItems:
          type: array
          items:
            $ref: "#/components/schemas/ServiceRequiredItem"
          default: []
      required:
        - name
        - estimatedTime
        - price

    # Service Tasks
    ServiceTask:
      type: object
      properties:
        id:
          type: integer
          format: int32
          nullable: true
        serviceId:
          type: integer
          format: int32
        workOrderId:
          type: integer
          format: int32
          nullable: true
        status:
          type: string
          enum:
            - PENDING_APPROVAL
            - APPROVED
            - REJECTED
            - IN_EXECUTION
            - COMPLETED
            - CANCELED
        estimatedTime:
          type: integer
          format: int32
          minimum: 1
          description: Estimated time in minutes
        price:
          type: number
          minimum: 0
          description: Service price frozen in the task (up to two decimal places)
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
      required:
        - serviceId
        - status
        - estimatedTime
        - price

    CreateServiceTaskRequest:
      type: object
      properties:
        serviceId:
          type: integer
          format: int32
          minimum: 1
        workOrderId:
          type: integer
          format: int32
          minimum: 1
      required:
        - serviceId
        - workOrderId

    StartServiceExecutionRequest:
      type: object
      properties:
        startedAt:
          type: string
          format: date-time
          description: Optional; if omitted, the current server time is used.
      additionalProperties: false

    CompleteServiceTaskRequest:
      type: object
      properties:
        completedAt:
          type: string
          format: date-time
      additionalProperties: false

    # Work Orders
    CreateWorkOrderCustomerRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        document:
          type: string
          minLength: 1
          description: CPF or CNPJ (formatted input accepted; normalized on persistence)
        phone:
          type: string
          minLength: 1
          description: Customer phone string (format is not strictly validated at API boundary)
        email:
          type: string
          format: email
        role:
          type: string
          enum:
            - customer
      required:
        - name
        - document
        - phone
        - email
        - role
      additionalProperties: false

    CreateWorkOrderVehicleRequest:
      type: object
      properties:
        plate:
          type: string
          minLength: 1
        brand:
          type: string
          minLength: 1
        model:
          type: string
          minLength: 1
        year:
          type: integer
          format: int32
          minimum: 1900
      required:
        - plate
        - brand
        - model
        - year
      additionalProperties: false

    CreateWorkOrderPartRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
        name:
          type: string
          minLength: 1
        description:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int32
          minimum: 0
        unitOfMeasure:
          type: string
          nullable: true
        price:
          type: number
          minimum: 0
      required:
        - sku
        - name
        - quantity
        - price
      additionalProperties: false

    CreateWorkOrderServicePartReferenceRequest:
      type: object
      properties:
        sku:
          type: string
          minLength: 1
        quantity:
          type: integer
          format: int32
          minimum: 1
      required:
        - sku
        - quantity
      additionalProperties: false

    CreateWorkOrderServiceRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        estimatedTime:
          type: integer
          format: int32
          minimum: 1
        price:
          type: number
          minimum: 0
        requiredParts:
          type: array
          items:
            $ref: "#/components/schemas/CreateWorkOrderServicePartReferenceRequest"
          description: Optional list; defaults to an empty list when omitted.
          default: []
      required:
        - name
        - estimatedTime
        - price
      additionalProperties: false

    WorkOrderServiceTask:
      type: object
      properties:
        serviceTaskId:
          type: integer
          format: int32
        status:
          type: string
          enum:
            - PENDING_APPROVAL
            - APPROVED
            - REJECTED
            - IN_EXECUTION
            - COMPLETED
            - CANCELED
        amount:
          type: number
          minimum: 0
          description: Service amount (up to two decimal places)
      required:
        - serviceTaskId
        - status
        - amount

    WorkOrder:
      type: object
      properties:
        id:
          type: integer
          format: int32
          nullable: true
        vehicleId:
          type: integer
          format: int32
        status:
          type: string
          enum:
            - RECEIVED
            - DIAGNOSIS
            - WAITING_APPROVAL
            - READY
            - IN_EXECUTION
            - FINALIZED
            - DELIVERED
            - CANCELED
        totalAmount:
          type: number
          minimum: 0
          description: >-
            Total work order amount (up to two decimal places), including both
            the base value of services and the value of stock parts/items
            associated with service tasks currently billable
            (excluding REJECTED and CANCELED tasks).
        publicToken:
          type: string
        publicTokenExpiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        serviceTasks:
          type: array
          items:
            $ref: "#/components/schemas/WorkOrderServiceTask"
      required:
        - vehicleId
        - status
        - totalAmount
        - publicToken
        - publicTokenExpiresAt
        - createdAt
        - updatedAt
        - serviceTasks

    CreateWorkOrderRequest:
      type: object
      properties:
        customer:
          $ref: "#/components/schemas/CreateWorkOrderCustomerRequest"
        vehicle:
          $ref: "#/components/schemas/CreateWorkOrderVehicleRequest"
        parts:
          type: array
          items:
            $ref: "#/components/schemas/CreateWorkOrderPartRequest"
          minItems: 1
        services:
          type: array
          items:
            $ref: "#/components/schemas/CreateWorkOrderServiceRequest"
          minItems: 1
      required:
        - customer
        - vehicle
        - parts
        - services
      additionalProperties: false

    WorkOrderStatusResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - RECEIVED
            - DIAGNOSIS
            - WAITING_APPROVAL
            - IN_EXECUTION
            - FINALIZED
            - DELIVERED
            - CANCELED
      required:
        - status

    ExternalWebhookWorkOrderEventRequest:
      discriminator:
        propertyName: eventType
        mapping:
          SERVICE_TASK_APPROVED: "#/components/schemas/ExternalWebhookServiceTaskApprovedEventRequest"
          SERVICE_TASK_REJECTED: "#/components/schemas/ExternalWebhookServiceTaskRejectedEventRequest"
          WORK_ORDER_STATUS_UPDATED: "#/components/schemas/ExternalWebhookWorkOrderStatusUpdatedEventRequest"
      oneOf:
        - $ref: "#/components/schemas/ExternalWebhookServiceTaskApprovedEventRequest"
        - $ref: "#/components/schemas/ExternalWebhookServiceTaskRejectedEventRequest"
        - $ref: "#/components/schemas/ExternalWebhookWorkOrderStatusUpdatedEventRequest"

    ExternalWebhookServiceTaskApprovedEventRequest:
      type: object
      properties:
        eventId:
          type: string
          minLength: 1
        eventType:
          type: string
          enum:
            - SERVICE_TASK_APPROVED
        workOrderId:
          type: integer
          format: int32
          minimum: 1
        serviceTaskId:
          type: integer
          format: int32
          minimum: 1
        occurredAt:
          type: string
          format: date-time
      required:
        - eventId
        - eventType
        - workOrderId
        - serviceTaskId
      additionalProperties: false

    ExternalWebhookServiceTaskRejectedEventRequest:
      type: object
      properties:
        eventId:
          type: string
          minLength: 1
        eventType:
          type: string
          enum:
            - SERVICE_TASK_REJECTED
        workOrderId:
          type: integer
          format: int32
          minimum: 1
        serviceTaskId:
          type: integer
          format: int32
          minimum: 1
        occurredAt:
          type: string
          format: date-time
      required:
        - eventId
        - eventType
        - workOrderId
        - serviceTaskId
      additionalProperties: false

    ExternalWebhookWorkOrderStatusUpdatedEventRequest:
      type: object
      properties:
        eventId:
          type: string
          minLength: 1
        eventType:
          type: string
          enum:
            - WORK_ORDER_STATUS_UPDATED
        workOrderId:
          type: integer
          format: int32
          minimum: 1
        targetStatus:
          type: string
          enum:
            - DIAGNOSIS
            - WAITING_APPROVAL
            - IN_EXECUTION
            - FINALIZED
            - CANCELED
            - DELIVERED
        serviceTaskId:
          type: integer
          format: int32
          minimum: 1
          description: Required when `targetStatus` is `IN_EXECUTION` or `FINALIZED`.
        occurredAt:
          type: string
          format: date-time
      required:
        - eventId
        - eventType
        - workOrderId
        - targetStatus
      additionalProperties: false

    ExternalWebhookWorkOrderEventResponse:
      type: object
      properties:
        eventId:
          type: string
        eventType:
          type: string
          enum:
            - SERVICE_TASK_APPROVED
            - SERVICE_TASK_REJECTED
            - WORK_ORDER_STATUS_UPDATED
        result:
          type: string
          enum:
            - processed
            - duplicate
      required:
        - eventId
        - eventType
        - result

    # Vehicles
    Vehicle:
      type: object
      properties:
        id:
          type: integer
          format: int32
          nullable: true
        plate:
          type: string
        brand:
          type: string
        model:
          type: string
        year:
          type: integer
          format: int32
          minimum: 1900
        ownerPersonId:
          type: integer
          format: int32
      required:
        - plate
        - brand
        - model
        - year
        - ownerPersonId

    # People
    Person:
      type: object
      properties:
        id:
          type: integer
          format: int32
          nullable: true
        name:
          type: string
        document:
          type: string
        phone:
          type: string
        email:
          type: string
        role:
          type: string
          enum: [customer, mecanic, front-desk]
      required:
        - name
        - document
        - phone
        - email
        - role

    CreatePersonRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        document:
          type: string
          minLength: 1
        phone:
          type: string
          minLength: 1
        email:
          type: string
          format: email
        role:
          type: string
          enum: [customer, mecanic, front-desk]
      required:
        - name
        - document
        - phone
        - email
        - role

    UpdatePersonRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
        document:
          type: string
          minLength: 1
        phone:
          type: string
          minLength: 1
        email:
          type: string
          format: email
        role:
          type: string
          enum: [customer, mecanic, front-desk]
      additionalProperties: false

    CreateVehicleRequest:
      type: object
      properties:
        plate:
          type: string
          minLength: 1
        brand:
          type: string
          minLength: 1
        model:
          type: string
          minLength: 1
        year:
          type: integer
          format: int32
          minimum: 1900
        ownerPersonId:
          type: integer
          format: int32
          minimum: 1
      required:
        - plate
        - brand
        - model
        - year
        - ownerPersonId
      example:
        plate: ABC1D23
        brand: Fiat
        model: Fiat Uno
        year: 2015
        ownerPersonId: 1

    UpdateVehicleRequest:
      type: object
      properties:
        plate:
          type: string
          minLength: 1
        brand:
          type: string
          minLength: 1
        model:
          type: string
          minLength: 1
        year:
          type: integer
          format: int32
          minimum: 1900
        ownerPersonId:
          type: integer
          format: int32
          minimum: 1
      additionalProperties: false
      example:
        plate: ABC1D23
        brand: Fiat
        model: Fiat Uno Turbo
        year: 2018
        ownerPersonId: 2

    # Errors
    VehicleValidationErrorResponse:
      type: object
      description: Validation error specific to vehicle routes.
      properties:
        error:
          type: string
          description: Short validation error message.
          example: Validation failed
        details:
          type: array
          description: List of validation errors (ZodIssue)
          items:
            type: object
            properties:
              path:
                type: array
                items:
                  oneOf:
                    - type: string
                    - type: integer
              message:
                type: string
              code:
                type: string
            additionalProperties: true
      required:
        - error
        - details

    DomainErrorResponse:
      type: object
      properties:
        error:
          type: string
          description: Domain error name
          example: SkuAlreadyExists
        message:
          type: string
          example: Stock item with this SKU already exists
      required:
        - error
        - message

    UnexpectedErrorResponse:
      type: object
      properties:
        error:
          type: string
          example: UnexpectedError
        message:
          type: string
      required:
        - error
        - message

    SimpleErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
